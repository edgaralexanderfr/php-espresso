#ifndef UTILS_H
#define UTILS_H

#define count_uint16(array) (sizeof(array) / sizeof(uint16_t))
#define count_string(array) (sizeof(array) / sizeof(string))
#define count(array) (sizeof(array) / sizeof(string))

#endif
